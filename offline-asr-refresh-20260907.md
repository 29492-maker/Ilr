# Offline speech recognition tools refresh

Rebuild the existing generic whisper.cpp runtime and public English speech model using the repository's existing CI workflow.

This branch contains no recordings, transcripts, private information, or credentials. Audio processing is performed locally after retrieving the generic build artifact. No changes to the main branch are required.
